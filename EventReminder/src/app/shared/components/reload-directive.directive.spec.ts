import { ReloadDirective } from './reload-directive.directive';

describe('ReloadDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ReloadDirective();
    expect(directive).toBeTruthy();
  });
});
